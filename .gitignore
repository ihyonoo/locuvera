# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Node / Frontend
node_modules/
dist/
build/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables and secrets
.env
.env.*
!.env.example
!.env.production.example

# Local artifacts
.codex
*.log
backups/
.logs/
.local-backups/
.postgres-data/
.postgres-data-migration-backup/
AGENTS.md
frontend/DESIGN.md

# 로컬 상태 스냅샷 / DB 덤프 (대용량·민감 데이터, 저장소 대상 아님)
# database/schema.sql 은 스키마 원본이므로 제외되지 않는다.
*.tgz
*_dump.sql

# OS / Editor
.DS_Store
._*
Thumbs.db
.idea/
.vscode/
.gstack/

# 로컬 전용 프로젝트 지침 (공유 저장소에는 올리지 않음)
# AGENTS.md가 원본, CLAUDE.md는 그것을 가리키는 심볼릭 링크 (AGENTS.md는 위 38행)
CLAUDE.md

# 문서 디렉터리 (논문·저작권·발표 자료 등 로컬 전용)
# README에 임베드하는 이미지는 assets/readme/ 에 따로 두고 커밋한다
/docs/

# 배포 런북 (홈서버 tailnet/사설망 IP가 들어 있어 공개 저장소에 올리지 않음)
/deploy/deployment.md
# 평가 트레이스는 수 GB이고 시드로 재생성 가능하다. 결과 수치는 eval/results/에 남긴다.
eval/runs/
